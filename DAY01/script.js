/*Async VS Defer

Use Async with script tag when the script has no
dependencies in the html.

Use Defer for spped & when there are html dependencies
- JS is downloading in parallel with html and DOM 
Building
- Script executes only after HTML and DOM building is done
*/
console.log('Hello, 40 days of JS!');
