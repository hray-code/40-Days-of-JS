This is my first script the for 40 days of Javascript Challenge. 

I've learned about the different ways to load and execute Javascript, by either adding the script tag at the end of the HTML body, using the async attribute, or using the defer attribute. 

I've added my external script in the HTML head tag using the defer attribute. The script is expected to display in the browser console. 